<% for column in Event.content_columns %>
<p>
  <% if column.human_name != "Announcement" && column.human_name != "Deleted" %>
  <b><%= column.human_name %>:</b> <%=h @event.send(column.name) %>
  <% end %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @event %> |
<%= link_to 'Back', :action => 'list' %>
